// types/options.ts

export type Option = {
  code: string;
  goods_options: number[];
  id: number;
  level: string;
  models_options: number[];
  name: string;
  sort: number | null;
  values: number[];
};